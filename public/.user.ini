; Per-directory PHP overrides (Apache + mod_php / PHP-FPM may apply this for scripts under public/).
; Default post_max_size=8M drops the entire upload when total POST exceeds ~8 MB (file + form + boundaries).
upload_max_filesize = 100M
post_max_size = 105M
max_execution_time = 300
max_input_time = 300
